#include <stdio.h>
// #include "pico/stdlib.h"
// #include "hardware/pio.h"
// #include "neopixel.h"

// Size of the ws2812 board
#define ROWS 16
#define COLS 16

int main()
{
    // initHardware();
    // setBrightness(0.2);

    // // Use led_remap to remap the hardware from a serial sequence to a 2D array
    // // So that lights can be addressed in [row][col] form
    // int** led_remap = remap(ROWS, COLS);

    // // Use setPixelColor
    // rainbow(0.1);

}
